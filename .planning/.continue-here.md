---
context: default
phase: 01-foundation-contracts-m0-setup
status: active
last_updated: 2026-05-31
branch: planning
commit: 4c40fa9
---

# Resume Here — Umraa (2026-05-31, resume session)

`planning` is green and synced. Tree clean.

## ⚠️ STANDING DIRECTIVE (Bader, 2026-05-31): DO NOT MERGE PRs right now.
Review/build is fine, but **do not merge** open PRs. If you find something actionable, **create a PR and leave it QUEUED** (open, unmerged) for review. (PR #79 below was created under this directive.)

## Merged this resume session (6 PRs)
#59 PDF connector · #62+#70 Claude auto-review (live on every non-draft PR) · #63 observability · #68 deployment (Cloud Run LIVE, `DEPLOY_ENABLED=true`) · **#72 AI reasoning core** (Gemini call + deterministic citation verifier; hardened via adversarial review — APIError abstention, per-item parse resilience, line-anchored tag regex, output-language prompt rules; reconciled the author's convergent ServerError/path fixes). Every closed issue is Done on Project #3.

## AI-application skills run on the reasoning core (Phase 2 docs)
- `.planning/phases/02-hour-6-vertical-slice-m1/02-AI-SPEC.md` — design/eval/guardrail contract (google-genai; clinical-CDS domain rubric Beers/STOPP/FDA; 8-dim eval strategy).
- `.planning/phases/02-hour-6-vertical-slice-m1/02-EVAL-REVIEW.md` — **47/100, SIGNIFICANT GAPS**. 1 COVERED / 3 PARTIAL / 4 MISSING. Top blockers: (1) `run_reasoning` has NO production caller — `/packet` serves the static `cache/store.py` stub; (2) no deterministic output-language guard (now partially addressed by PR #79); (3) no structured eval harness / golden dataset.

## Open PRs (queued — DO NOT MERGE per directive)
- **#79 (mine, QUEUED)** — deterministic output-language guard (AI-SPEC dim 3 / #75). 100% gate. Needs team review of the banned-phrase set before merge.
- **#71** Patient intake app (#24) — ⚠️ backend CI was FAILING; needs a fix.
- **#74** frontend doctor result card (#19) — not reviewed.
- **#76** demo patient (planted interaction) — not reviewed.

## Open follow-up issues
- **#73** surface partial-data notice into `/packet`+UI (blocked on reasoning-core wiring).
- **#75** deterministic prose-language guard (PR #79 = first slice) + per-clause grounding + **wire `run_reasoning` into `/packet`** (the EVAL-REVIEW #1 blocker).
- #65/#66/#67 hardening. LOW: `@claude review` on-demand trigger; deploy.yml `GITHUB_TOKEN`/Secret-Manager + a `paths:` filter (docs-only pushes currently trigger a redeploy).

## Anti-patterns (do NOT repeat)
Coverage ONLY on py3.12 (`/tmp/umraa-py312`); CI runs ruff+black BEFORE pytest (lint fails first); `git fetch && git reset --hard origin/planning` after each merge before doc commits; no AI-trace watermarks ("claude" as the Action product name is fine); teammates may push to a PR branch mid-review — `git pull`/merge before pushing; the review action skips self-modifying-workflow PRs by design.

## Next action
Respect the no-merge directive. Options: review #71 (fix its failing CI), #74, #76 and queue fixes; or build out the EVAL-REVIEW remediation (wire `run_reasoning`→`/packet`, eval harness/golden set) as queued PRs. Do NOT merge until Bader lifts the directive.
