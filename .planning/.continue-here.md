# ⏸️ Continue Here — Umraa (Polypharmacy Decision Packet)

**Paused:** 2026-05-31
**Branch:** `planning` @ `8b01fc1` (synced with origin, working tree clean)
**Phase:** 1 of 5 — Foundation & Contracts (M0 Setup) — ~93%

---

## Current Position

Phase 1 / M0 nearly complete. Working the team's GitHub-issue PRs on protected `planning`, one at a time, full flow: **code review + test run + Copilot bot review → formal approve → admin squash-merge → close** (memory `pr-merge-flow-umraa`).

Repo is now **squash-only** (set this session): squash merge only, merge-commit + rebase disabled, auto-delete-branch on merge enabled.

## Completed This Session (all merged to `planning`)

#47 (#21 seeds) · #48 (#5 FHIR subset/minimize/refs) · #53 (#21 fix-forward: ACB RxCUIs re-derived from RxNav + SOURCES.md provenance) · #52 (#6 flattener FHIR-03/04) · #50 (#28 HL7v2 scaffold) · #51 (#17 packet API; black fix pushed to Hamza's branch with his OK) · **#57 (100% core coverage + CI gate `--cov-fail-under=100` on fhir/providers/cache/knowledge/reasoning; deleted dead `fhir/flattener.py`)** · **#54 (#9 docker-compose backend service; issue #9 closed)**.

## ⏸️ NEXT — 4 OPEN PRs, reviewed-pending, NONE merged (paused for Bader's go)

Queue grew right at pause (#58, #59 appeared):

1. **PR #55 (#12 cache, `ha/issue-12`, Hamza) — THE REAL Postgres cache.** Reconcile with #51's in-memory stand-in `backend/app/cache/store.py` (don't double-build). Gate requires `app/cache` at 100%. Bader wanted to eyeball the overlap first. **M1 §22 prerequisite.**
2. **PR #56 (#14 openFDA drug-safety, `feat/drug-safety-openfda-14`)** — reasoning/knowledge runtime; gate requires `app/knowledge`+`app/reasoning` at 100%.
3. **PR #58 (#11 MockFHIR connector, `ha/issue-11`, Hamza)** — CONN-02, **M1 §22 slice prerequisite**. (Earlier I wrongly thought #51 imported a missing mock_fhir module — it didn't; #58 is the real MockFHIR work.)
4. **PR #59 (#27 PDF connector, `feat/pdf-connector`)** — char-offset provenance; Phase 4/M3 (CONN-04).

**Resume action:** wait for Bader's "go" (NOT given at pause), then full review→merge flow in dependency order — **#58 MockFHIR + #55 cache first** (M1 prerequisites; reconcile #55 vs the stand-in), then #56, then #59. Each touching gated packages must ship 100% coverage or CI fails.

## After the Queue — Phase 1 → Phase 2 transition

When the queue lands and M0 is genuinely complete (confirm M0 reqs FHIR-01..04, CONN-01/06, SEC-02/03, API-08, FE-07, INFRA-01/02/04, OPS-01; OPS-01 real-key provisioning may stay open), **run the PHASE-BOUNDARY REVIEW GATE** (standing rule; STATE Blockers + memory `umraa-phase-boundary-review-gate`):
1. `/gsd-code-review` → 2. `/gsd-verify-work` → 3. `/gsd-secure-phase` → 4. `/gsd-add-tests` (coverage done) → fix-forward → THEN advance to Phase 2.

## Gotchas / Decisions This Session

- **Coverage must be measured on Python 3.12** (CI runtime). Local 3.9 can't import the code (`UTC`, `dataclass(slots=True)`), reports a false ~13%. Throwaway venv at `/tmp/umraa-venv` (py3.12 + reqs) if it survives.
- **Always `git fetch && git reset --hard origin/planning` after every `gh pr merge`** before the next local `.planning` commit — squash merges happen server-side; a stale-branch push reverts the merge (memory `sync-local-before-committing-planning`).
- No-AI-traces mandate: commit/author as `B2707`; squash drops `Co-authored-by: Copilot Autofix` trailers (now structurally enforced — squash-only repo). Memory `umraa-project-setup`.
- The output channel was badly lagged all session — tool results arrived in delayed bursts; every git/gh action verified landed regardless.
- **This handoff overwrote a STALE `.continue-here.md`** from a prior session (it had described #47/#48 as still in-review). Trust THIS file + HANDOFF.json.

## Resume Command

`/gsd-resume-work` (or "continue") → then await "go" and review/merge **#58, #55, #56, #59**.
