---
context: default
phase: 01-foundation-contracts-m0-setup
status: paused
last_updated: 2026-05-31T08:12:44.541Z
branch: planning
commit: 0077fe3
---

# Resume Here — Umraa merge-queue (paused 2026-05-31)

`planning` is green and synced (`0077fe3`, 0/0 vs origin). No half-merged remote state.

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| False coverage on local Python 3.9 | Local `python3` is 3.9 and can't import the code (UTC/slots) → reports a false ~13%. | blocking | Measure coverage ONLY on Python 3.12. A venv exists at `/tmp/umraa-py312` (`pip install -r backend/requirements.txt`). CI runs 3.12. |
| CI silently skipped on conflicting PRs | `pull_request` CI runs on the merge ref; a PR with a merge conflict has no mergeable commit so checks show NOTHING (looks unrun). | blocking | If `gh pr checks N` is empty, run `gh pr view N --json mergeable`; if CONFLICTING, merge `origin/planning` into the branch and resolve. |
| Stale `.planning` push reverts a merge | `gh pr merge --squash` advances `origin/planning`; a later push from a stale local reverts it. | blocking | ALWAYS `git fetch && git reset --hard origin/planning` after each merge, before the next `.planning` doc commit. |
| AI-trace watermarks | Project policy: no `Co-Authored-By: Claude`, no robot emoji, no "Generated with Claude" in commits/code. | blocking | Plain conventional commits. ("claude" as the product name in PR #62/#69 is intentional and fine.) |
| Self-approving own PRs | `gh pr review --approve` fails on your own PRs. | advisory | Use `gh pr merge N --squash --admin --delete-branch` (admin bypasses the review requirement). |

<current_state>
Working the merge queue one PR at a time with: reconcile (merge planning) -> full gate on py3.12 -> Claude deep-review (+ adversarial Workflow for correctness-critical code) -> fix-forward -> admin squash-merge -> close issue -> STATE doc-sync.

**7 PRs merged this session** (#58 MockFHIR, #55 cache+refresh, #56 openFDA, #64 X-Cache /packet, #61 FE-07, #60 PostgresProvider, #69 SEC-02/HL7v2 hardening). The critical path is DONE: M0 complete · M1 §22-slice spine (MockFHIR->cache+refresh->openFDA->/packet with X-Cache) · M2 two-institution PostgresProvider · SEC-02 now fail-closed.

GSD config: **Opus for every agent** (model_overrides) + **deep** code-review.
</current_state>

<remaining_work>
- **#59 PDF connector (M3)** — local WIP committed on `feat/pdf-connector` (planning merged + lint fixed), NOT pushed. Fails gate: `pdf.py` 92% (12 uncovered lines) AND emits `MedicationRequest` (NOT in the 6-resource subset; flatten/reason expect `MedicationStatement`). Needs remap -> MedicationStatement (valid R4B) + tests to 100%. OR post findings to Vivek.
- **#62 Claude auto-review CI (mine)** — BLOCKED on human action (install Claude GitHub App + add secret); then merge.
- **#63 observability (M4)** — not reviewed.
- **#68 deployment (Hamza)** — DRAFT, not ready.
</remaining_work>

<blockers>
- #62 auto-review is inert until the Claude GitHub App + an auth secret are installed (admin/OAuth — human only).
- #59 fails the 100% gate + emits the wrong med resource type.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full machine-readable state (open PRs, follow-ups #65/#66/#67, human actions).
2. `.planning/STATE.md` — narrative state + this session's merges.
3. `.planning/phases/01-foundation-contracts-m0-setup/01-VALIDATION.md` and `01-REVIEW.md` — the M0 audit + boundary-gate report.

## Infrastructure State
- `planning` @ `0077fe3`, synced, green. py3.12 venv at `/tmp/umraa-py312`.
- Open follow-up issues: #65 (openFDA), #66 (cache->API), #67 (PostgresProvider).

<next_action>
Start with PR #59: `git switch feat/pdf-connector` (local WIP is there) -> remap `MedicationRequest`->`MedicationStatement` (valid R4B) in `providers/pdf.py` -> cover the 12 `pdf.py` lines to 100% on py3.12 -> push -> admin squash-merge. OR post #59's findings (MedicationRequest + coverage) to Vivek and move to #62's human steps / #63.
</next_action>
