# Continue Here — Umraa session handoff

**Paused:** 2026-05-31 · **Phase 1 (M0 Foundation & Contracts) — ~50%** · branch `planning` (default dev branch)

## ⚡ Resume in one line
New session → say **"review the open PRs"**. Two are waiting: **#47** (Vivek, drug-knowledge datasets, issue #21) and **#48** (Mohammad, FHIR subset builders, issue #5). Both need the full review pass before merge.

## How to process each PR (the standing loop)
For every open PR, in order:
1. **Check it doesn't touch `.planning/`** (only @B2707 changes those — CODEOWNERS-gated).
2. **AI-trace scan** — commit messages + PR body for `Co-Authored-By: Claude` / `🤖 Generated with Claude Code` / "anthropic". If found: amend+force-push the branch to strip, and edit the PR body. (This has happened twice — always check.)
3. **Real code review** of the diff (correctness/tests/quality) — not just CI. Post findings as a comment.
4. **Confirm CI green.**
5. **Merge** with `gh pr merge <n> --squash --admin --delete-branch` + a clean `--subject`/`--body` (admin override bypasses the review gate; we're the owner). Use `Closes #<issue>` only if the issue is truly complete.
6. **Close the issue / move board** to Done; move newly-unblocked issues to Ready.
7. **Push the `.planning/STATE.md` update directly to `planning`** (owner can direct-push `.planning`).

## State of the board
- **Done:** #1 (R4B spike), #3 (Provider ABC), #4 (DTO/OpenAPI contract), #7 (CI). Plus PR #46 (#9 config-guard code) and PR #49 (deploy pipeline) merged.
- **In review (PRs open):** #47→#21 (datasets, Vivek), #48→#5 (FHIR subset, Mohammad).
- **In progress:** #9 (Hamza) — config guard merged; **needs real Gemini/openFDA keys** in local `.env` to fully close (then unblocks #14/#15).
- **Ready:** #5 (PR #48 open), #12 (cache), #23 (Postgres), #28 (HL7v2), #33 (observability).
- **Top remaining bottleneck after #5 lands:** the cache→API chain (#12 → #17) and the reasoning chain (#5→#6→#15).

## Non-negotiable rules (enforced)
- **No AI/Claude traces anywhere** — commits, PR bodies, code, docs. Authored as @B2707.
- **`planning` is protected:** teammates must PR + 1 review; **`enforce_admins=false`** so the owner can bypass/direct-push. **`.planning/` is owned by @B2707 only** (CODEOWNERS `/.planning/ @B2707` + code-owner review). I push `.planning` updates directly; everything else (code) goes through PRs.
- **Code-review every PR before merge** (saved feedback rule).
- `main` = submission branch (promote `planning → main` when verified). `docs/PRD.md` + `README.md` stay; everything GSD/planning lives in `.planning/`.

## Open threads / decisions pending
- **Keys:** notes posted on #14/#15 telling Mohammad/Vivek to use their own free Gemini+openFDA keys locally so they aren't blocked. Hamza owns docker `env_file` + Cloud Run secrets (#9/#35), values never in repo.
- **Deploy pipeline (#49 merged):** Dockerfiles + `deploy.yml` build→GHCR→Artifact Registry→Cloud Run on `planning` merges, **DORMANT** until Hamza does GCP setup in `docs/DEPLOY.md` and sets repo var `DEPLOY_ENABLED=true`. Decisions: SA-key auth, both on Cloud Run, seeded-local DB.
- **Possible cleanup the user raised:** move `CONTRIBUTING.md` + `docs/DEPLOY.md` into `.planning/`? Flagged downsides (CONTRIBUTING is GitHub-recognized at root; DEPLOY.md is referenced by `deploy.yml`/#35). Awaiting decision — if yes, move + fix references.

## Environment reminders
- Real repo is the **`umraa/` subdir**: `/Users/baderasadi/MPC-Hacks/project/umraa`. (User also has a 2nd clone at `…/badertask/umraa`.)
- gh authed as B2707 (admin) with `repo, workflow, project, read:org` scopes. Project board #3: https://github.com/users/B2707/projects/3
- Full durable context in memory: `umraa-project-setup`, `pr-review-requires-code-review`.

---
*Resume: `/clear`, open a fresh session, then "review the open PRs".*
