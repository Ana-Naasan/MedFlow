---
context: default
phase: 01-foundation-contracts-m0-setup
status: active
last_updated: 2026-05-31
branch: planning
commit: 5559ace
---

# Resume Here — Umraa merge-queue (2026-05-31, resume session)

`planning` is green and synced (`5559ace`, 0/0 vs origin). Tree clean.

## What this session did

- **Merged #59 PDF connector (CONN-04, issue #27 closed, card→Done).** Fix-forward earned by a 4-lens adversarial review workflow:
  1. meds emitted `MedicationRequest` (NOT in the 6-resource subset → silently dropped by flatten/reason) → remapped to **valid-R4B `MedicationStatement`** (status+subject+dosage).
  2. review caught that `flatten._dose()` read only structured `doseQuantity`, so the PDF's free-text dose was **dropped before reasoning** → added a `dosage[].text` fallback in `backend/app/fhir/flatten.py` (dose/route/indication now reaches the Gemini context; verified end-to-end on the planted Warfarin+Aspirin pair).
  3. latent provenance-invariant break (snippet `.strip()`ed vs unstripped offsets) → `_trimmed_span` helper keeps `page_text[start:end] == snippet`.
  4. test fixture path was CWD-relative (broke under CI's `cd backend`) → anchored to `__file__`.
  `pdf.py` 92%→**100%**; full gate 100% on py3.12; CI green.
- **Merged #62 + #70 Claude auto-review Action.** `.github/workflows/claude-code-review.yml` now reviews **every non-draft PR** automatically (opened/synchronize/reopened/ready_for_review — no reviewer assignment). Default branch = `planning`, `CLAUDE_CODE_OAUTH_TOKEN` secret installed → the prior human-action blocker is **resolved**.
- **Board reconciled** (Project #3 "Umraa Build"): every closed issue is in Done (moved #14/#13/#23/#27 this session).

## Critical anti-patterns (do NOT repeat)

| Pattern | Prevention |
|---------|------------|
| False coverage on local Python 3.9 | Measure coverage ONLY on py3.12. venv at `/tmp/umraa-py312`. CI runs 3.12. |
| CI silently skipped on conflicting PRs | If `gh pr checks N` is empty, `gh pr view N --json mergeable`; if CONFLICTING, merge `origin/planning` into the branch. |
| Stale `.planning` push reverts a merge | ALWAYS `git fetch && git reset --hard origin/planning` after each merge before the next `.planning` doc commit. |
| AI-trace watermarks | Plain conventional commits — no `Co-Authored-By: Claude`, no robot emoji. ("claude" as the Action's product name is intentional and fine.) |
| Self-approving own PRs | `gh pr merge N --squash --admin --delete-branch`. |
| Review action skips self-modifying workflow PRs | By design (anti-exfiltration: workflow must match default branch). Not a failure. |

## Open queue
- **#63 (#33 observability, Bader)** — structured logging + partial-data warnings (M4). Not reviewed; will auto-review on next push. Do the human merge-gate too (see memory `pr-merge-flow-umraa`).
- **#68 (deployment, Hamza)** — DRAFT; review when un-drafted.
- Follow-ups (non-blocking): #65 openFDA hardening, #66 cache→API, #67 PostgresProvider hardening. LOW: wire `@claude review` on-demand comment trigger (`issue_comment` event) into the review workflow.

## Required reading
1. `.planning/STATE.md` — full narrative + this session's merges.
2. `.planning/phases/01-foundation-contracts-m0-setup/01-REVIEW.md` + `01-VALIDATION.md` — M0 audit/gate.

## Next action
Review/merge **#63** (full human merge-gate: code review → tests on py3.12 → reconcile Copilot + the new Claude auto-review → approve → admin squash-merge → close + card→Done). Then confirm M0 completeness and run the PHASE-BOUNDARY REVIEW GATE before advancing to Phase 2.
