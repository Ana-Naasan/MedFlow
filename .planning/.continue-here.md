---
context: default
phase: 02-hour-6-vertical-slice-m1
status: paused
last_updated: 2026-05-31T10:35Z
branch: planning
commit: 0d3729b
---

# ⏸️ Continue Here — Umraa merge-queue (paused 2026-05-31)

`planning` is green and synced (`0d3729b`). Tree clean. Cloud Run deploy is LIVE (`DEPLOY_ENABLED=true`).

## ⚠️ Merge preference (nuanced — read first)
Bader said "stop doing PRs for now" then overrode it with "unblock my teammates," so I merged the teammate-blocking PRs (#76, #77). **Default: do NOT merge without an explicit go from Bader.** Unblocking teammates was a one-time authorization. If you find something actionable, create a PR and **queue** it (open, unmerged).

## Merged this session (8 PRs)
#59 PDF connector · #62+#70 Claude auto-review (live on every non-draft PR) · #63 observability · #68 deployment (Cloud Run live) · **#72 reasoning core** (Gemini + deterministic citation verifier; hardened via adversarial review) · **#76 demo patient + intake/hypotheses endpoints** · **#77 cache-authoritative citation verifier (#16)**. Every closed issue is Done on Project #3.

## AI-application skills run (Phase-2 docs)
- `.planning/phases/02-hour-6-vertical-slice-m1/02-AI-SPEC.md` — reasoning-core design/eval/guardrail contract.
- `.planning/phases/02-hour-6-vertical-slice-m1/02-EVAL-REVIEW.md` — **47/100, SIGNIFICANT GAPS**. #1 blocker: **`run_reasoning` has NO production caller — `/packet` still serves the static `cache/store.py` stub**, so the reasoning core + verifier run on zero requests.

## Open PRs
- **#79 (mine) — DO NOT TOUCH.** Output-language guard. Bader said he "already has something working on 79"; likely a duplicate — decide with him whether to close it.
- **#78 (Mohammad, RxNav)** — he's actively finishing it. Lint/format already fixed (by me) + reconciled; punch-list posted (PR #78 comment): 2 F841 unused vars + 9 uncovered lines in `app/knowledge/rxnav.py` (79-81 `_get_client` lazy-init, 109 no-candidates, 137-138 non-dict skip, 155-157 `_fetch_ingredient` IN-group).
- **#71 (Hamza, old intake)** — superseded by #76; commented recommending close.

## Open follow-up issues
- **#75** — deterministic prose-language guard (PR #79 = first slice) + per-clause grounding + **WIRE `run_reasoning` INTO `/packet`** (the EVAL-REVIEW #1 blocker; highest-value remaining work).
- **#73** — surface partial-data notice into `/packet`+UI (blocked on the wiring above).
- #65/#66/#67 hardening. LOW: `@claude review` on-demand trigger; deploy.yml `GITHUB_TOKEN`/Secret-Manager + a `paths:` filter (docs pushes currently redeploy).

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Prevention | Severity |
|---------|------------|----------|
| False coverage on local Python 3.9 | Measure coverage ONLY on py3.12 (`/tmp/umraa-py312`) | blocking |
| Merging over red CI | CI runs ruff+black BEFORE pytest — lint fails first; reproduce the gate locally | blocking |
| Stale `.planning` push reverts a merge | `git fetch && git reset --hard origin/planning` after every merge before doc commits | blocking |
| AI-trace watermarks | Plain conventional commits ("claude" as the Action product name is intentional/fine) | blocking |
| Self-approving own PRs | Own PR → `gh pr merge N --squash --admin --delete-branch`; teammate PR → `gh pr review N --approve` first | advisory |
| Teammates push mid-review | `git pull`/merge before pushing; expect concurrent pushes to PR branches | advisory |
| Skipping adversarial review on correctness-critical code | Run the adversarial-review Workflow for connectors→FHIR / verifier / reasoning core — it has repeatedly caught real defects 100% coverage masked | blocking |
| Projects board status drift | A Projects automation occasionally re-flips a closed card to "In review" — re-set closed issues to Done and re-verify | advisory |

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full machine-readable state.
2. `.planning/STATE.md` — narrative.
3. `.planning/phases/02-hour-6-vertical-slice-m1/02-EVAL-REVIEW.md` — the reasoning-core eval gaps + #1 blocker (the wiring).

## Infrastructure State
- `planning` @ `0d3729b`, synced, green. py3.12 venv at `/tmp/umraa-py312`. Cloud Run deploy LIVE on every planning push.

## Next action
Confirm with Bader: close PR #79 or keep. Then (with his go) the highest-value work is **#75: wire `run_reasoning` into `/packet`** so the reasoning core + verifier actually run on requests. #78 lands when Mohammad finishes coverage; close #71.
