---
context: phase
phase: 02-hour-6-vertical-slice-m1
task: null
total_tasks: null
status: in_progress
last_updated: 2026-05-31T09:09:47-04:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through actual failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Verify every push landed on the RIGHT branch.** I once pushed a #92 fix to a brand-new stray branch instead of the PR's head, so the PR didn't update. Mitigation: after every push, compare `git rev-parse HEAD` to `gh pr view <n> --json headRefOid` and require MATCH=YES before claiming the fix is on the PR.
- [ ] CONSTRAINT: **Never report a commit SHA you haven't confirmed via git/gh.** Earlier I cited fabricated SHAs (`5e9d2a1`, `42d2191`, `b8f3c9e`, `4d2bdf5`) that never existed, and once claimed #80 merged before it had. Always capture the REAL merge SHA from `gh pr view <n> --json mergeCommit` and verify objects with `git cat-file -t`.
- [ ] CONSTRAINT: **Merge only on a CLEAN review PASS** (not a non-blocking CONCERNS). Fix the findings on-branch until the re-review is clean, then merge.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale-base local testing | Testing the raw PR head locally gave a FALSE test failure because the head was 8 commits behind `planning` (missing the verifier completeness-propagation from #84/#93). CI tests the *merged* state, so it was green. | blocking | Before local verification of any PR, `git merge origin/planning` into the branch (or rebase) so you test the true merge state. |
| `-k` filter hides failures | Running `pytest -k pipeline` showed all-green while a different test (`test_packet.py::test_packet_includes_completeness`) was actually failing. | blocking | Run the FULL core gate (no `-k`) before declaring green: the exact CI command from `backend/`. |
| Trusting empty bash output | The bash stdout buffer dropped results repeatedly this session; empty returns were NOT proof of success/failure. | advisory | Write command output to a `/tmp` file and `Read` it; re-verify state from files, not from a blank return. |
| Long sleep polling loops | Chained `sleep`s to wait on CI made the agent appear hung ("everything is waiting"); the harness now blocks chained sleeps. | advisory | One status check per turn; if CI is pending, schedule a single ~270s wakeup and stop. |
| Re-running the GitHub reviewer's dimensions | Spun up a local adversarial-review workflow that duplicated the GitHub Claude reviewer's correctness/security/contract/tests pass. | advisory | Read the GitHub review verdict; only add what it can't do (local run, cross-PR integration, conflict resolution). See memory `trust-github-claude-review`. |

<current_state>
Pausing mid PR-merge-queue. Goal: review + merge the team's green PRs, each to a CLEAN Claude-review PASS, one at a time (they overlap on the `/packet` path).

On local branch `pr-92` = PR #92 head `feat/issue-75-wire-reasoning-into-packet` @ `5e8824f` (local==remote verified). Working tree clean. `planning` HEAD at pause ≈ `da1cf0b` (top of the 7 merges below).
</current_state>

<completed_work>

Merged this session (all real SHAs, on `planning`):
- #79 → `f17c5af` (output-language guard, partial #75)
- #78 → `a8d85f3` (RxNav, #20)
- #81 → `3505edd` (intake idempotency, #24)
- #82 → `2a99348` (PDF highlighting + completeness panel, #31)
- #80 → `b7c615c` (keyboard a11y, #32) — fixed 2 HIGH (openapi-fetch error handling + page-action tests) before merge
- #84 → `6f0d411` (unknown≠none, #30)
- #86 → `da1cf0b` (DDInter, #22)

Issues closed: #20, #24, #30, #31, #32, #22 (+ earlier #16/#18/#19/#27/#33/#35). Board reconciled, all closed-issue cards → Done.

#92 fix DONE + pushed (commit `5e8824f`): broadened `build_reasoned_packet` `except KeyError`→`except Exception` (any reasoning failure abstains to the citation-safe scaffold, not HTTP 500); corrected false "verify_packet drops completeness" comments; merged current `planning` in. Local gate at push: ruff+black clean, 590 passed, 100% coverage.
</completed_work>

<remaining_work>

1. **#92** (mine, #75) — CI was `review=pending backend=pass` at pause. When review resolves: if clean PASS + mergeable → `gh pr review 92 --approve` then `gh pr merge 92 --squash --admin --delete-branch`. Verify #75 auto-closes; move its board card to Done.
2. **#91** (Hamza, #73) — NOT fixed. HIGH: partial→clean notice never clears (`FetchMetadata` row upserted with `ttl_seconds=None`, never deleted → stale gaps forever). Fix `backend/app/api/intake.py` to delete/clear the row on a clean re-intake + add a partial→clean test. MEDIUM: dedupe the duplicated `"FetchMetadata"` literal across `intake.py`/`packet.py`. Then merge on clean PASS.
3. **#83** (Vivek, #25) — NOT fixed. HIGH: committed `.planning/.continue-here-reviewer.md` leaks a personal email — `git rm` it from the branch + add `.planning/.continue-here*` to `.gitignore`. MEDIUM: inert patient-fact chips render as interactive buttons — make non-interactive (or restore resolve-on-click) + file an FE-02 follow-up. Frontend gate green (npm ci/typecheck/test/lint). Then merge on clean PASS.
4. **#93** (mine, #87/#88) — review re-running after the data_gaps revert + verifier/demo-honesty tests. If clean PASS + mergeable → merge; close #87/#88; board Done.
5. **#89** (Vivek, #26) — SKIP: `review=fail` AND `backend=fail`. Leave for author.
6. After all merges: update `.planning/STATE.md` with the REAL merge SHAs; commit + push.
</remaining_work>

<decisions_made>
- Merge bar = a **clean** Claude-review PASS (not a non-blocking CONCERNS) — per the user.
- Don't re-run the GitHub reviewer's own dimensions; only add what it can't do.
- Keep DEMO-001 `data_gaps` as curated hand-authored strings (the data-driven signal rides on the confidence downgrade + completeness list) — the demo must read honestly; "don't lie on the demo".
- "Downgrade" = the existing REASON-05 confidence-lowering when safety-critical data is absent. #93/#84 made it data-driven; the rule and demo behavior are unchanged (not a spec compromise).
</decisions_made>

<blockers>
- GitHub Claude `review` check intermittently hangs in_progress (hung twice today). Workaround: if a review run sits >25 min in_progress, `gh run cancel <id>` then `gh run rerun <id>` once.
- Local bash stdout buffer unreliable this session → write to `/tmp` and `Read`.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable twin of this file (PR list, SHAs, next action).
2. `.planning/STATE.md` — broader project state (note: still lists some already-merged PRs as open; update at end of merge run).
3. Memory `trust-github-claude-review` — the merge-bar + don't-double-review rule.

## Infrastructure State
- Local venv: repo-root `.venv` (python3.12); `greenlet` + `aiosqlite` pip-installed into it (NOT in requirements.txt; needed locally for the async-DB tests that CI runs fine).
- Exact CI gate (run from `backend/`): `../.venv/bin/ruff check . && ../.venv/bin/black --check . && ../.venv/bin/python -m pytest -o addopts="" --cov=app/fhir --cov=app/providers --cov=app/cache --cov=app/knowledge --cov=app/reasoning --cov-fail-under=100 -q`
- Project board: `PVT_kwHOBROmas4BZQ7B` (Umraa Build, #3); Status field `PVTSSF_lAHOBROmas4BZQ7BzhUQ0OU`; Done option `c81d710d`.
- Branch model: `planning` = protected dev branch (admin squash-merge via `--admin`); self-approve is refused (author), so `--admin` carries the merge.

<context>
Working the merge queue one PR at a time. The user explicitly wants every merged PR to pass a clean Claude review AND to "work perfectly" — no quality compromise, and the demo must be honest. Fix real findings properly (make claims true with tests), don't paper over them. The three remaining PRs overlap on `/packet`, so merge sequentially and rebase/merge `planning` into each before verifying.
</context>

<next_action>
Start with: `gh pr checks 92 --json name,bucket` (write to /tmp, Read it). If `review` is still pending, schedule one ~270s wakeup and stop. When it resolves, read the verdict from `gh api repos/B2707/umraa/issues/92/comments`; if it's a clean PASS and #92 is mergeable, approve + admin squash-merge, then capture the real merge SHA and move to #91.
</next_action>
