---
context: reviewer-integrator session (NOT phase execution)
status: paused
last_updated: 2026-05-31T11:01:52Z
branch_at_pause: fix/issue-24-intake-hardening
working_tree: clean
author_identity: B2707 (bader <baderasadi07@gmail.com>)
note: >
  This is a SEPARATE handoff for the review/fix/queue instance. It deliberately does
  NOT overwrite .planning/.continue-here.md or HANDOFF.json — those are owned and
  maintained by the OTHER instance (the planning-branch merge-flow / AI-spec instance).
  Do not commit this file to planning or to a feature PR branch.
---

# Continue Here — Reviewer/Integrator Session

## Role this session
PR review + fix-forward + **queue** (the other instance does the actual merging into `planning`).

## Standing rules (HARD — carried from the user)
- **Single writer to `planning`.** Do NOT merge into `planning` or push to it directly. Review → verify → leave PRs **approved/queued**; the other instance admin-merges. (One #74 merge slipped through earlier on prior authorization — landed cleanly, no conflict — then the rule was set: no more direct merges.)
- **Never clobber `.planning/.continue-here.md` / `HANDOFF.json`** — the other instance owns `.planning/` (CODEOWNERS) and is running an AI-spec research task there.
- **No AI traces** in commits/PRs: author as `B2707`, no `Co-Authored-By`, no 🤖, no "Claude". (Squash-merge also strips trailers.)
- **B2707 cannot self-approve a B2707-authored PR** → those rely on the other instance's admin squash-merge.
- Verify everything on **Python 3.12** via `/tmp/umraa-venv` (local 3.9 can't import the code). CI gate = `ruff` + `black --check` + `pytest --cov-fail-under=100` on `app/{fhir,providers,cache,knowledge,reasoning}`.

## Done this session
- **#69** merged — Phase-1 (M0) boundary review gate report + SEC-02 fail-closed allow-list + HL7v2 no-PID guard.
- **#63** merged — observability (structured logging + partial-data warnings, #33).
- **#62** merged — Claude auto-review CI (ultracode fan-out profile, OAuth token). Live + working (it reviews PRs now).
- **#74** merged — doctor result card + citation modal (#19). Verified; non-blocking follow-ups noted on the PR (modal a11y, typed payloads, 404 test).
- **#77** verified + (merged by other instance) — citation verifier (#16); black-format was the only fix.
- **#79** verified GREEN + queued — output-language guard (no "caused by"/"stop drug X"). Posted sign-off comment; needs admin-merge.
- **#81** open + queued — #24 intake hardening: idempotent confirm/dismiss + deterministic seeding. 470 tests, 100% gate. **Mine; needs admin-merge.**
- **#71 closed** (superseded by #76 + #81) + branch deleted. Pruned 7 dead local branches + the local PR-71 review doc.

## Queued, awaiting the other instance's merge
- **#81** (fix/issue-24-intake-hardening) — verified green locally.
- **#79** (feat/reasoning-language-guard) — verified green locally.

## Next actions (not started — pick up here)
1. **#78 (RxNav, #20, Mohammad) has RED backend CI.** This is the **biggest teammate unblocker** — `#20 → unblocks #22 (DDInter) + #26 (Beers/ACB)` and the differential (#29). Diagnose the red CI, fix to green, queue. (Open PR, branch `feat/rxnav-20`.)
2. **WR-1 — wire the verifier into `GET /packet` (#16 → #17).** `cache/store.py:_build_default_packet` emits a `Patient/pat-001` citation that does NOT resolve for a non-DEMO patient, so `GET /packet` can serve an unresolvable citation — violates the no-unresolved-citation invariant. Correct fix: run `verify_packet(resource_lookup, evidence_lookup)` inside `get_packet` so unresolvable hypotheses are dropped / the packet abstains. Higher-stakes than intake polish. (Separate PR; touches `api/packet.py` + reasoning — verify it doesn't ripple into packet/store/coverage tests.)

## Resume
1. `git fetch origin --prune` (the other instance is actively merging into `planning`).
2. Re-check open PRs: `gh pr list --repo B2707/umraa --state open`.
3. Branch any new work off the LATEST `origin/planning`. Run the gate on `/tmp/umraa-venv` before pushing.
4. Open a PR; do NOT merge — queue it.
